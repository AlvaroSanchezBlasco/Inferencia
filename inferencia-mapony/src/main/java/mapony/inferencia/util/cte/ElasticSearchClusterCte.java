package mapony.inferencia.util.cte;

public class ElasticSearchClusterCte {

	/**
	 * <ul>
	 * <li>[ES] Constante del fichero de propiedades que indica el tipo que vamos a crear en ElasticSearch
	 * <li>[EN] Name of the type that we have to create on the ElasticSearch cluster.
	 * </ul>
	 */
	public static final String typeName = "type_name";

	/**
	 * <ul>
	 * <li>[ES] Constante del fichero de propiedades que indica el nombre del indice que vamos a crear en ElasticSearch
	 * <li>[EN] Index of the File/path to process in the Job execution.
	 * </ul>
	 */
	public static final String indexName = "index_name";

	/**
	 * <ul>
	 * <li>[ES] Constante del fichero de propiedades que indica el nombre del cluster en el que vamos a hacer la carga de datos en ElasticSearch
	 * <li>[EN] Name of the ElasticSearch Cluster
	 * </ul>
	 */
	public static final String clusterName = "clusterName";

	/**
	 * <ul>
	 * <li>[ES] Constante del fichero de propiedades que indica la ip del cluster de ElasticSearch
	 * <li>[EN] Ip of the ElasticSearch Cluster
	 * </ul>
	 */
	public static final String ip = "ip";

	/**
	 * <ul>
	 * <li>[ES] Constante del fichero de propiedades que indica el puerto del cluste de ElasticSearch
	 * <li>[EN] Port of the ElasticSearch Cluster
	 * </ul>
	 */
	public static final String port = "port";

}
